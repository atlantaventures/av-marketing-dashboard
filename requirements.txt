# AV Dashboard — Python dependencies
# Install with: pip install -r requirements.txt

# GA4 data pull (ga4_pull.py)
google-analytics-data
google-auth-oauthlib

# Eventbrite pull (eventbrite_pull.py — generated by Claude when ready)
requests
